<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.BoardMapper">

    <!-- 게시글 등록 -->
    <insert id="insertBoard" parameterType="com.example.board.domain.BoardV0">
        INSERT INTO t_board (title, content, writerNm, regDate, delYn)
        VALUES (#{title}, #{content}, #{writerNm}, NOW(), 'N')
    </insert>

    <!-- 게시글 목록 조회 -->
    <select id="selectBoardList" resultType="com.example.board.domain.BoardV0">
        SELECT 
            idx,
            title,
            content,
            writerNm,
            regDate,
            delYn
        FROM t_board 
        WHERE delYn = 'N'
        ORDER BY regDate DESC
    </select>

</mapper>
